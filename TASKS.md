Let's work on the following tasks:

[x] Splash page (`splash-screen.tsx`)
   - [x] Let's use only the #f0f0f0 color as a vertex and remove all the blueish colors from the design. Use only white and #f0f0f0 as the background color.
   - [x] Use 'Space Grotesk' font for the text in all page.
   - [x] Remove the dark red color from the bottom of the loading and texts and use the `theme.ts` colors of the primary color.  
[x] Sticky slider section (`sticky-slider-section.tsx`)
   - [x] Use 'Space Grotesk' font for the text in all page.
   - [x] Let's remove the background of the parent menu active and use the border with having the breathing opacity with the red (255 54 54) and midjourney orange color instead of the dark red.
   - [x] Let's use the `theme.ts` colors of the primary color for the active menu item.
   

Once tasks are completed, mark them 'x' in the TASKS.md file.